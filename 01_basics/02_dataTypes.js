"use strict"; // treat all js code as newer version 

//alert("") this will work in browser better

// make code readable please ashu

let name = "hitesh"
let age = 18
let isLoggedIn = false

// number, bigint, string, boolean, null => standalone, undefined, symbol, 

// whats the type of null 
console.log(typeof null);
console.log(typeof undefined);